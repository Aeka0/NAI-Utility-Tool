## 变更内容

- 简要说明这次修改解决了什么问题

## 验证方式

- [ ] 已本地构建 `dotnet build .\NAITool.sln -c Debug -p:Platform=x64`
- [ ] 如涉及发布逻辑，已验证 `.\publish.ps1`
- [ ] 如涉及文档或使用方式，已同步更新 README / 相关文档

## 自查清单

- [ ] 没有提交 `user/`、`output/`、`logs/`、`build/`、`publish/`
- [ ] 没有提交 API Token、配置密钥、个人隐私数据
- [ ] 改动范围与标题、描述一致

## 关联问题

Closes #
